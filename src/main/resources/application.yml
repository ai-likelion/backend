server:
  port: ${BACKEND_WEB_PORT:8078}

spring:
  application:
    name: ai-teacher-a
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/likelion?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${MYSQL_USERNAME:likelion}
    password: ${MYSQL_PASSWORD:likelion}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:nnnnnn}
